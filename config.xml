<!-- Config file for the engine -->
<config>
  <engine>
    <title value="Platformer Game"/>
    <targetFrameRate value="60"/>
  </engine>

  <render>
    <vsync value="false"/>
  </render>

  <window>
    <resolution width="1280" height="720" scale="1"/>
    <fullscreen value="false"/>
    <borderless value="false"/>
    <resizable value="false"/>
    <fullscreen_window value="false"/>
  </window>

  <!-- L10: TODO 3: Add a <player> section: texture path, animations tsx
       path, movement speed/jumpForce, frame width/height. Initial position
       stays in code (it's map data, not player config; L15 loads it from
       the map instead), and so do animation clip names (idle/move/jump --
       they come from the TSX's own tile layout, not from config.xml). -->
</config>
